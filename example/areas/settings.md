# Settings

The settings tab and the underlying settings storage.

**Files:**
- `src/views/SettingsTab.ts` — UI
- Settings interface + defaults — declared in `src/main.ts`
- Persistence — `loadData()` / `saveData()` (writes to plugin's `data.json`)

**Backlinks:** Obsidian fills the section below with every commit that mentioned `[[settings]]`. Don't write here manually.
